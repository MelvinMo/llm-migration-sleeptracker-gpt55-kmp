<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- MIGRATION: The React Native app uses the same dark product palette.
         The launcher icon background keeps the migrated KMP build visually
         tied to the sleep tracker UI instead of using Android's default icon. -->
    <solid android:color="#1A1A2E" />
</shape>
